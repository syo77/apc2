{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "${workspaceFolder}/memoria_dinamica/heap.c",  // Substitua pelo nome do seu arquivo
                "-o",
                "${workspaceFolder}/memoria_dinamica/a.out"    // Nome do executável
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "build"
        }
    ]
}
